{{#each this.products}}
<div class="col-md-3 col-sm-6 gameBox">
    <div class="thumbnail">
        <img src="{{imgURL}}" alt="empty">
        <div class="caption">
            <h3>{{title}}</h3>
            <p class="itemPrice">
                {{price}}
            </p>
            <p>
                <a href="#" class="btn btn-primary">Buy Now!</a> <a href="#" class="btn btn-default">More Info</a>
            </p>
        </div>
    </div>
</div>
{{/each}}
